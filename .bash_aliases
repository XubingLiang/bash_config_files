# aliases for ls
alias ls="ls -aF --color=always" # the -f flag causes ls to sort case insensitively (works only on mac)
alias lsl="ls -alF --color=always" # the -f flag causes ls to sort case insensitively (works only on mac)
